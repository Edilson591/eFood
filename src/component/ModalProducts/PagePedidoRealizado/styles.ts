import styled from "styled-components";

export const HeaderPedidoRealizado = styled.h3`
  font-weight: 700;
  font-size: 1.6rem;
  color: ${(props) => props.theme.colors.backgroundSecundary};
  padding-top: 2.4rem;
  margin-bottom: 1.6rem;
  `;

export const DescritionPedidoRealizado = styled.p`
  font-weight: 400;
  color: ${(props) => props.theme.colors.backgroundSecundary};
  font-size: 1.4rem;
  margin-bottom: 2.4rem;
`;
